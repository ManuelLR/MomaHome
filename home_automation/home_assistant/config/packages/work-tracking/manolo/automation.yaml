# automation:

  # ─────────────────────────────────────────────────────────────────────
  # AUT-Work Manolo - Signal ON
  # Fires when any individual work signal turns ON.
  # Invariant: events never overlap. Before creating the new ⏰ event,
  # any currently active event (⏰ or 💼) is closed at the exact start
  # time of the new event. This ensures querying "right now" always
  # yields exactly one event.
  # Requires: calendar.manolo_work (Local Calendar — create via HA UI)
  # Requires: HA 2024.2+ for calendar.get_events with response_variable
  # ─────────────────────────────────────────────────────────────────────
  - alias: "AUT-Work Manolo - Signal ON"
    id: aut_work_manolo_signal_on
    mode: single
    initial_state: 'on'
    triggers:
      - platform: state
        entity_id: switch.man_001
        to: 'on'
      - platform: state
        entity_id: binary_sensor.s23_work_profile
        to: 'on'
    actions:
      - variables:
          # Capture the exact moment the signal fired — used as both the
          # close-time for any existing event and the start of the new one.
          start_str: "{{ now().strftime('%Y-%m-%d %H:%M:%S') }}"
          end_placeholder_str: "{{ (now().timestamp() + 82800) | timestamp_custom('%Y-%m-%d %H:%M:%S', true) }}"
          start_hm: "{{ now().strftime('%H:%M') }}"
          # 1-second window to find the event active right now
          one_sec_later: "{{ (now().timestamp() + 1) | timestamp_custom('%Y-%m-%d %H:%M:%S', true) }}"

      # Find any currently active event (one without ✓ in summary)
      - action: calendar.get_events
        target:
          entity_id: calendar.manolo_work
        data:
          start_date_time: "{{ start_str }}"
          end_date_time: "{{ one_sec_later }}"
        response_variable: current_events

      - variables:
          existing_uid: >
            {% set events = current_events['calendar.manolo_work']['events'] | default([]) %}
            {% set ns = namespace(uid='') %}
            {% for e in events %}
              {% if '✓' not in (e.summary | default('')) and ns.uid == '' %}
                {% set ns.uid = e.uid | default('') %}
              {% endif %}
            {% endfor %}
            {{ ns.uid }}

      # Close the existing event at exactly start_str (no gap, no overlap)
      - if:
          - condition: template
            value_template: "{{ existing_uid != '' }}"
        then:
          - action: calendar.update_event
            target:
              entity_id: calendar.manolo_work
            data:
              uid: "{{ existing_uid }}"
              end_date_time: "{{ start_str }}"

      # Create the new pending event
      - action: calendar.create_event
        target:
          entity_id: calendar.manolo_work
        data:
          summary: "⏰ ¿Trabajando?"
          start_date_time: "{{ start_str }}"
          end_date_time: "{{ end_placeholder_str }}"
          description: "Pendiente de confirmar"

      - delay:
          seconds: 2

      # Retrieve the UID of the newly created event to embed in Telegram buttons
      - action: calendar.get_events
        target:
          entity_id: calendar.manolo_work
        data:
          start_date_time: "{{ start_str }}"
          end_date_time: "{{ (now().timestamp() + 30) | timestamp_custom('%Y-%m-%d %H:%M:%S', true) }}"
        response_variable: new_events

      - variables:
          new_uid: >
            {% set events = new_events['calendar.manolo_work']['events'] | default([]) %}
            {% set ns = namespace(uid='') %}
            {% for e in events %}
              {% if e.summary | default('') == '⏰ ¿Trabajando?' and ns.uid == '' %}
                {% set ns.uid = e.uid | default('') %}
              {% endif %}
            {% endfor %}
            {{ ns.uid }}

      - action: script.telegram_notify
        data:
          target_person: manolo
          title: "⏰ ¿Estás trabajando?"
          message: "Actividad detectada a las {{ start_hm }}"
          inline_keyboard:
            - "✅ Sí:/work_start_yes_manolo {{ new_uid }}"
            - "❌ No:/work_start_no_manolo {{ new_uid }}"


  # ─────────────────────────────────────────────────────────────────────
  # AUT-Work Manolo - Start YES
  # User confirmed they are working. Updates the ⏰ event to 💼/🏖️.
  # The UID comes from the Telegram button command argument.
  # ─────────────────────────────────────────────────────────────────────
  - alias: "AUT-Work Manolo - Start YES"
    id: aut_work_manolo_start_yes
    mode: single
    initial_state: 'on'
    triggers:
      - platform: event
        event_type: telegram_callback
        event_data: &work_start_yes_manolo
          command: "/work_start_yes_manolo"
      - platform: event
        event_type: telegram_command
        event_data: *work_start_yes_manolo
    actions:
      - variables:
          uid: "{{ trigger.event.data.args[0] | default('') }}"
          day_label: >
            {{ '🏖️ Trabajo (especial)' if is_state('binary_sensor.workday_sensor', 'off') else '💼 Trabajo' }}

      - action: calendar.update_event
        target:
          entity_id: calendar.manolo_work
        data:
          uid: "{{ uid }}"
          summary: "{{ day_label }}"
          description: "Sesión confirmada"

      - condition: template
        value_template: "{{ trigger.event.event_type == 'telegram_callback' }}"
      - action: telegram_bot.answer_callback_query
        data:
          callback_query_id: "{{ trigger.event.data.id }}"
          message: "¡Sesión iniciada! ✓"


  # ─────────────────────────────────────────────────────────────────────
  # AUT-Work Manolo - Start NO
  # User denied working. Delete the ⏰ event.
  # ─────────────────────────────────────────────────────────────────────
  - alias: "AUT-Work Manolo - Start NO"
    id: aut_work_manolo_start_no
    mode: single
    initial_state: 'on'
    triggers:
      - platform: event
        event_type: telegram_callback
        event_data: &work_start_no_manolo
          command: "/work_start_no_manolo"
      - platform: event
        event_type: telegram_command
        event_data: *work_start_no_manolo
    actions:
      - variables:
          uid: "{{ trigger.event.data.args[0] | default('') }}"

      - action: calendar.delete_event
        target:
          entity_id: calendar.manolo_work
        data:
          uid: "{{ uid }}"

      - condition: template
        value_template: "{{ trigger.event.event_type == 'telegram_callback' }}"
      - action: telegram_bot.answer_callback_query
        data:
          callback_query_id: "{{ trigger.event.data.id }}"
          message: "Sesión cancelada"


  # ─────────────────────────────────────────────────────────────────────
  # AUT-Work Manolo - Signal OFF
  # Fires when any individual work signal turns OFF (30s debounce).
  # Closes the currently active event (whether ⏰ or 💼) with the
  # signal-off timestamp. If the other signal is still ON, triggers
  # Signal ON logic (creates a new ⏰ and asks again).
  # ─────────────────────────────────────────────────────────────────────
  - alias: "AUT-Work Manolo - Signal OFF"
    id: aut_work_manolo_signal_off
    mode: single
    initial_state: 'on'
    triggers:
      - platform: state
        entity_id: switch.man_001
        to: 'off'
        for:
          seconds: 30
      - platform: state
        entity_id: binary_sensor.s23_work_profile
        to: 'off'
        for:
          seconds: 30
    actions:
      - variables:
          signal_off_str: "{{ now().strftime('%Y-%m-%d %H:%M:%S') }}"
          one_sec_before: "{{ (now().timestamp() - 1) | timestamp_custom('%Y-%m-%d %H:%M:%S', true) }}"

      # Find the event that was active just before the signal turned off
      - action: calendar.get_events
        target:
          entity_id: calendar.manolo_work
        data:
          start_date_time: "{{ one_sec_before }}"
          end_date_time: "{{ signal_off_str }}"
        response_variable: active_events

      - variables:
          active_uid: >
            {% set events = active_events['calendar.manolo_work']['events'] | default([]) %}
            {% set ns = namespace(uid='') %}
            {% for e in events %}
              {% if '✓' not in (e.summary | default('')) and ns.uid == '' %}
                {% set ns.uid = e.uid | default('') %}
              {% endif %}
            {% endfor %}
            {{ ns.uid }}
          active_summary: >
            {% set events = active_events['calendar.manolo_work']['events'] | default([]) %}
            {% set ns = namespace(summary='') %}
            {% for e in events %}
              {% if '✓' not in (e.summary | default('')) and ns.summary == '' %}
                {% set ns.summary = e.summary | default('') %}
              {% endif %}
            {% endfor %}
            {{ ns.summary }}
          # Confirmed events (💼/🏖️) get ✓ appended; pending ⏰ events keep their summary
          new_summary: >
            {% set s = active_summary %}
            {% if '💼' in s or '🏖️' in s %}
              {{ s + ' ✓' }}
            {% else %}
              {{ s }}
            {% endif %}
          other_signal_on: >
            {% if trigger.entity_id == 'switch.man_001' %}
              {{ is_state('binary_sensor.s23_work_profile', 'on') }}
            {% else %}
              {{ is_state('switch.man_001', 'on') }}
            {% endif %}

      # Close the active event with the signal-off timestamp
      - if:
          - condition: template
            value_template: "{{ active_uid != '' }}"
        then:
          - action: calendar.update_event
            target:
              entity_id: calendar.manolo_work
            data:
              uid: "{{ active_uid }}"
              end_date_time: "{{ signal_off_str }}"
              summary: "{{ new_summary }}"

      # If another signal is still on, start a new session detection
      - if:
          - condition: template
            value_template: "{{ other_signal_on == 'True' or other_signal_on == True }}"
        then:
          - action: automation.trigger
            target:
              entity_id: automation.aut_work_manolo_signal_on
            data:
              skip_condition: false


  # ─────────────────────────────────────────────────────────────────────
  # AUT-Work Manolo - Split midnight
  # If a confirmed 💼 session is active at midnight, split it:
  # close the current event and create a new one starting at midnight.
  # Runs at exactly 00:00:00.
  # ─────────────────────────────────────────────────────────────────────
  - alias: "AUT-Work Manolo - Split midnight"
    id: aut_work_manolo_split_midnight
    mode: single
    initial_state: 'on'
    triggers:
      - platform: time
        at: "00:00:00"
    actions:
      - variables:
          midnight_str: "{{ now().strftime('%Y-%m-%d 00:00:00') }}"
          new_end_str: "{{ (now().timestamp() + 82800) | timestamp_custom('%Y-%m-%d %H:%M:%S', true) }}"
          one_sec_later: "{{ (now().timestamp() + 1) | timestamp_custom('%Y-%m-%d %H:%M:%S', true) }}"

      - action: calendar.get_events
        target:
          entity_id: calendar.manolo_work
        data:
          start_date_time: "{{ midnight_str }}"
          end_date_time: "{{ one_sec_later }}"
        response_variable: midnight_events

      - variables:
          midnight_uid: >
            {% set events = midnight_events['calendar.manolo_work']['events'] | default([]) %}
            {% set ns = namespace(uid='') %}
            {% for e in events %}
              {% if ('💼' in (e.summary | default('')) or '🏖️' in (e.summary | default('')))
                 and '✓' not in (e.summary | default('')) and ns.uid == '' %}
                {% set ns.uid = e.uid | default('') %}
              {% endif %}
            {% endfor %}
            {{ ns.uid }}
          midnight_summary: >
            {% set events = midnight_events['calendar.manolo_work']['events'] | default([]) %}
            {% set ns = namespace(s='') %}
            {% for e in events %}
              {% if ('💼' in (e.summary | default('')) or '🏖️' in (e.summary | default('')))
                 and '✓' not in (e.summary | default('')) and ns.s == '' %}
                {% set ns.s = e.summary | default('') %}
              {% endif %}
            {% endfor %}
            {{ ns.s }}

      - if:
          - condition: template
            value_template: "{{ midnight_uid != '' }}"
        then:
          # Close the old event at midnight
          - action: calendar.update_event
            target:
              entity_id: calendar.manolo_work
            data:
              uid: "{{ midnight_uid }}"
              end_date_time: "{{ midnight_str }}"
              summary: "{{ midnight_summary + ' ✓' }}"

          # Open a new confirmed event for the new day
          - action: calendar.create_event
            target:
              entity_id: calendar.manolo_work
            data:
              summary: "{{ midnight_summary }}"
              start_date_time: "{{ midnight_str }}"
              end_date_time: "{{ new_end_str }}"
              description: "Continuación de sesión anterior"


  # ─────────────────────────────────────────────────────────────────────
  # AUT-Work Manolo - Compact yesterday
  # Runs at 00:30 to process yesterday's full set of events.
  # Merges consecutive confirmed (✓) sessions where the gap between
  # end of one and start of the next is ≤ 2 minutes.
  # Uses repeat:count to handle multiple mergeable pairs.
  # ─────────────────────────────────────────────────────────────────────
  - alias: "AUT-Work Manolo - Compact yesterday"
    id: aut_work_manolo_compact_yesterday
    mode: single
    initial_state: 'on'
    triggers:
      - platform: time
        at: "00:30:00"
    actions:
      - variables:
          yesterday_start: "{{ (today_at('00:00') | as_timestamp - 86400) | timestamp_custom('%Y-%m-%d 00:00:00', true) }}"
          yesterday_end: "{{ (today_at('00:00') | as_timestamp - 1) | timestamp_custom('%Y-%m-%d %H:%M:%S', true) }}"

      # Up to 10 merge passes (each pass merges one pair; 10 is well above typical daily sessions)
      - repeat:
          count: 10
          sequence:
            - action: calendar.get_events
              target:
                entity_id: calendar.manolo_work
              data:
                start_date_time: "{{ yesterday_start }}"
                end_date_time: "{{ yesterday_end }}"
              response_variable: compact_events

            - variables:
                # Find the first consecutive pair of ✓ events with gap ≤ 2 min
                compact_curr_uid: >
                  {% set events = compact_events['calendar.manolo_work']['events'] | default([]) %}
                  {% set confirmed = [] %}
                  {% for e in events %}
                    {% if '✓' in (e.summary | default('')) %}
                      {% set confirmed = confirmed + [e] %}
                    {% endif %}
                  {% endfor %}
                  {% set sorted_ev = confirmed | sort(attribute='start') %}
                  {% set ns = namespace(uid='') %}
                  {% for i in range(sorted_ev | length - 1) %}
                    {% if ns.uid == '' %}
                      {% set curr = sorted_ev[i] %}
                      {% set next = sorted_ev[i+1] %}
                      {% set gap = (as_timestamp(next.start) - as_timestamp(curr.end)) %}
                      {% if gap <= 120 %}
                        {% set ns.uid = curr.uid | default('') %}
                      {% endif %}
                    {% endif %}
                  {% endfor %}
                  {{ ns.uid }}
                compact_next_uid: >
                  {% set events = compact_events['calendar.manolo_work']['events'] | default([]) %}
                  {% set confirmed = [] %}
                  {% for e in events %}
                    {% if '✓' in (e.summary | default('')) %}
                      {% set confirmed = confirmed + [e] %}
                    {% endif %}
                  {% endfor %}
                  {% set sorted_ev = confirmed | sort(attribute='start') %}
                  {% set ns = namespace(uid='', found=false) %}
                  {% for i in range(sorted_ev | length - 1) %}
                    {% if not ns.found %}
                      {% set curr = sorted_ev[i] %}
                      {% set next = sorted_ev[i+1] %}
                      {% set gap = (as_timestamp(next.start) - as_timestamp(curr.end)) %}
                      {% if gap <= 120 %}
                        {% set ns.uid = next.uid | default('') %}
                        {% set ns.found = true %}
                      {% endif %}
                    {% endif %}
                  {% endfor %}
                  {{ ns.uid }}
                compact_new_end: >
                  {% set events = compact_events['calendar.manolo_work']['events'] | default([]) %}
                  {% set confirmed = [] %}
                  {% for e in events %}
                    {% if '✓' in (e.summary | default('')) %}
                      {% set confirmed = confirmed + [e] %}
                    {% endif %}
                  {% endfor %}
                  {% set sorted_ev = confirmed | sort(attribute='start') %}
                  {% set ns = namespace(end='', found=false) %}
                  {% for i in range(sorted_ev | length - 1) %}
                    {% if not ns.found %}
                      {% set curr = sorted_ev[i] %}
                      {% set next = sorted_ev[i+1] %}
                      {% set gap = (as_timestamp(next.start) - as_timestamp(curr.end)) %}
                      {% if gap <= 120 %}
                        {% set ns.end = next.end %}
                        {% set ns.found = true %}
                      {% endif %}
                    {% endif %}
                  {% endfor %}
                  {{ ns.end }}

            # If a pair was found, merge them
            - if:
                - condition: template
                  value_template: "{{ compact_curr_uid != '' }}"
              then:
                # Extend the first event to cover the second
                - action: calendar.update_event
                  target:
                    entity_id: calendar.manolo_work
                  data:
                    uid: "{{ compact_curr_uid }}"
                    end_date_time: "{{ compact_new_end }}"
                # Delete the second event (now merged)
                - action: calendar.delete_event
                  target:
                    entity_id: calendar.manolo_work
                  data:
                    uid: "{{ compact_next_uid }}"
